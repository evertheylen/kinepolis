Alle klassen ./struct hebben de volgende methodes:
+insert(element)
+delete(key)
+retrieve(key)
+inorder()

insert(element) moet het element op de juist plaats zetten. Dit wordt gedaan op basis van de zoeksleutel.

delete(key) moet het element met zoeksleutel 'key' verwijderen uit de data structuur.

Hoe de datastructuren weten welk attribuut als zoeksleutel dient, wordt gespecifieerd bij het 
creëeren van de datastructuur (in de functie createDataStructure()).

De initializer zal dan ook de nodige informatie krijgen om te weten op welk attribuut er gesorteerd 
moet worden; initializers zouden een minimale functie __init__() moeten implementeren die er als volgt
uitziet:

    def __init__(self, attribute):
        self.array = []
        self._attribute = attribute

